// Get React and react DOM
import React from 'react';
import { render } from 'react-dom';
// create component and produce HTML.
const App = function () {
    return <p>Start App</p>
}

// Take component html and show on browser

render(App);